import React from "react";
import styles from "../styles/components/header.module.scss";

const Logo = () => {
  return <img src="./logo.svg" alt="Logo Kasa" className={styles.logo} />;
};

export default Logo;
